[package]
name = "spinhdl_core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
petgraph = { version = "0.8.3"}
toml = {version = "0.9.8"}
glob = {version = "0.3"}
